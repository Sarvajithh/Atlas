/**
 * Document View (§8.2.2): PDF/Lecture/Reference Book reader with
 * annotations and bookmarks, Assistant Panel docked right. Viewer
 * implementation (§44) is deferred to the UI implementation milestone.
 */
export function DocumentView() {
  return <section aria-label="Document View" />;
}
