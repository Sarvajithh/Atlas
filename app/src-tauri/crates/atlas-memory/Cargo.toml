[package]
name = "atlas-memory"
version.workspace = true
edition.workspace = true
publish.workspace = true

# atlas-memory: Student Memory domain logic (§7.3, §14, §19). Defines
# repository interfaces for annotations, bookmarks, chat, revision history,
# learning progress, and analytics. Concrete storage is provided by atlas-db.

[dependencies]
atlas-types = { path = "../atlas-types" }
atlas-utils = { path = "../atlas-utils" }
atlas-events = { path = "../atlas-events" }
