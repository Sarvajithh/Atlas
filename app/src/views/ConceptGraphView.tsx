/**
 * Concept Graph View (§8.2.3): visual graph of extracted concepts and
 * their relations, filterable by workspace. Read-only query against
 * graph.get (§43.1); graph rendering is deferred to the UI implementation
 * milestone.
 */
export function ConceptGraphView() {
  return <section aria-label="Concept Graph View" />;
}
