/**
 * Activity Rail (§8.1): far-left icon strip -- Workspaces, Search, Concept
 * Graph, Memory/Analytics, Settings. Icon set and navigation wiring are
 * deferred to the UI implementation milestone.
 */
export function ActivityRail() {
  return <nav aria-label="Activity Rail" className="w-12 shrink-0 border-r" />;
}
