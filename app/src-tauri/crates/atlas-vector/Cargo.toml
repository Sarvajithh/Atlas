[package]
name = "atlas-vector"
version.workspace = true
edition.workspace = true
publish.workspace = true

# atlas-vector: Vector DB client abstraction (§5, §14). Implements the
# EmbeddingRepository interface defined by atlas-indexer against Qdrant or
# LanceDB in embedded/local mode. Namespaced per workspace (§22).

[dependencies]
atlas-types = { path = "../atlas-types" }
atlas-utils = { path = "../atlas-utils" }
atlas-indexer = { path = "../atlas-indexer" }
serde.workspace = true
serde_json.workspace = true
