/**
 * Quiz / Exam Mode (§8.2.5): full-focus mode, Assistant Panel hidden or
 * restricted (§8.3). Quiz flow is deferred to the UI implementation
 * milestone.
 */
export function QuizExamMode() {
  return <section aria-label="Quiz and Exam Mode" />;
}
