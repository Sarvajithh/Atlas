/**
 * Workspace Home (§8.2.1): list of linked workspaces (active + archived),
 * quick stats. This is the app's default landing screen (§8.3 -- never a
 * blank chat box). Data fetching via workspace.list (§43.1) is deferred to
 * the UI implementation milestone.
 */
export function WorkspaceHome() {
  return <section aria-label="Workspace Home" />;
}
