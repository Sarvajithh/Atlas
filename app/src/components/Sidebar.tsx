/**
 * Sidebar (§8.1): file tree of the active workspace's linked folder(s),
 * with indexing status badges per file. Tree rendering and IPC wiring are
 * deferred to the UI implementation milestone.
 */
export function Sidebar() {
  return <aside aria-label="Workspace file tree" className="w-64 shrink-0 border-r" />;
}
