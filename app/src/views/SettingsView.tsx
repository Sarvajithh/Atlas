/**
 * Settings (§8.2.7): Ollama connection, model-per-engine overrides,
 * indexing preferences, storage locations. Form wiring via settings.*
 * commands (§43.1) is deferred to the UI implementation milestone.
 */
export function SettingsView() {
  return <section aria-label="Settings" />;
}
